---
label: calendar
hide_body: true
fields:
- name: today_s_date
  type: datetime
  description: what is today
  config:
    required: false
    date_format: MM/DD/YYYY
    time_format: 
    display_utc: false
  label: Today's date
  default: now
- name: choose_color
  type: color
  label: choose color
  config:
    required: false
    color_format: Hex
