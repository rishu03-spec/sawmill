---
label: calendar
hide_body: true
fields:
- name: background_style
  type: select
  default: " Left"
  config:
    required: false
    options:
    - Brand Color
    - " Left"
    - " Dark"
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Background Style
- name: heading
  type: text
  config:
    required: false
  label: Heading
  description: heading goes here
- name: content
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Content
- name: today_s_date
  type: datetime
  description: what is today
  config:
    required: false
    date_format: MM/DD/YYYY
    time_format: 
    display_utc: false
  label: Today's date
  default: now
- name: choose_color
  type: color
  label: choose color
  config:
    required: false
    color_format: Hex
- name: our_features
  type: list
  config:
    use_select: false
    min: 
    max: 
    source:
      type: documents
  label: Our features
- name: calendar_position
  type: select
  default: " right"
  config:
    required: false
    options:
    - left
    - " right"
    - " bottom"
    - " top"
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Calendar Position
  description: place calendar in block
- name: color_picker_position
  type: select
  default: left
  config:
    required: false
    options:
    - left
    - " right"
    - " bottom"
    - " top"
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Color Picker Position
  description: Position of color picker
