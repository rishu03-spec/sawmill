---
label: calendar
hide_body: true
fields:
- name: today_s_date
  type: datetime
  description: what is today
  config:
    required: false
    date_format: MM/DD/YYYY
    time_format: 
    display_utc: false
  label: Today's date
  default: now
- name: choose_color
  type: color
  label: choose color
  config:
    required: false
    color_format: Hex
- name: our_features
  type: list
  config:
    use_select: false
    min: 
    max: 
    source:
      type: documents
  label: Our features
- name: background_style
  type: select
  default: " Left"
  config:
    required: false
    options:
    - Brand Color
    - " Left"
    - " Dark"
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Background Style
